public class TaxCalcForFemale : TaxCalculator
{
    public TaxCalcForFemale(decimal slab1Amount, decimal slab2Amount, decimal slab3Amount, int slab2TaxRate, int slab3TaxRate)
    {
        // Initialize properties based on parameters
    }
}
