using System;

class Program
{
    static void Main(string[] args)
    {
        try
        {
            XmlReaderApp xmlReaderApp = new XmlReaderApp();
            xmlReaderApp.Run(args);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"An unexpected error occurred: {ex.Message}");
        }
    }
}
